{
  "name": "Capitulo-Desafio-final",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /desafio-dwf-m5",
    "deploy": "gh-pages -d ./build",
    "build-n-deploy": "yarn build && yarn deploy"
  },
  "dependencies": {
    "gh-pages": "^4.0.0",
    "parcel": "^2.0.0-rc.0"
  },
  "devDependencies": {
    "@parcel/transformer-image": "2.0.0-rc.0"
  }
}
